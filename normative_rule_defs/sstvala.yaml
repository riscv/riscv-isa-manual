---
# yaml-language-server: $schema=../docs-resources/schemas/defs-schema.json

$schema: "../docs-resources/schemas/defs-schema.json#"

# Used to create normative rules for sstvala.adoc file.
# Tags refer to anchors in any adoc file for any ISA manual.

chapter_name: Sstvala Extension for Trap Value Reporting

normative_rule_definitions:

  - name: sstvala_stval_faulting_vaddr
    tags: ["norm:sstvala_stval_faulting_vaddr"]

  - name: sstvala_stval_faulting_instruction
    tags: ["norm:sstvala_stval_faulting_instruction"]
