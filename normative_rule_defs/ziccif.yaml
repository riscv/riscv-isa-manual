---
# yaml-language-server: $schema=../docs-resources/schemas/defs-schema.json

$schema: "../docs-resources/schemas/defs-schema.json#"

chapter_name: Ziccif Extension for Instruction-Fetch Atomicity

normative_rule_definitions:

  - name: ziccif_if_atomicity
    tags: ["norm:ziccif_if_atomicity"]

  - name: ziccif_fetch_mechanism
    tags: ["norm:ziccif_fetch_mechanism"]

  - name: ziccif_pma_application
    tags: ["norm:ziccif_pma_application"]
