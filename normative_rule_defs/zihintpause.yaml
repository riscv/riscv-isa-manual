---
# yaml-language-server: $schema=../docs-resources/schemas/defs-schema.json
#zihintpause.yaml

$schema: "../docs-resources/schemas/defs-schema.json#"

# Used to create normative rules for zhintpause.adoc file.
# Tags refer to anchors in any adoc file for any ISA manual.

chapter_name: Zihintpause (Pause)

normative_rule_definitions:
# 1 to 1 rule to tag
  - name: pause_op
    tags: ["norm:pause_op"]
  - name: pause_enc_fence
    summary: PAUSE instruction is encoded in FENCE instruction
    tags: ["norm:pause_enc_fence"]
