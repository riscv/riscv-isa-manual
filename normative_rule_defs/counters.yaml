$schema = "normative_rule_defs.json#"

normative_curations:
  - name: counter_registers
    tags: ["norm:zicntr_ziphm_registers"]

  - name: pseudoinstructions
    tags: ["norm:zicntr_rdcycle", "norm:zicntr_rdtime", "norm:zicntr_rdinstret"]

  - name: ziphm_counters
    tags: ["norm:ziphm_counters"]
