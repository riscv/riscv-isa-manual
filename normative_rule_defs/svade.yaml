---
# yaml-language-server: $schema=../docs-resources/schemas/defs-schema.json

$schema: "../docs-resources/schemas/defs-schema.json#"

# Used to create normative rules for supervisor.adoc file (Svade section).
# Tags refer to anchors in any adoc file for any ISA manual.

chapter_name: Svade Extension for Accessed and Dirty Bit Exceptions

normative_rule_definitions:

  - name: svade_access_ad_bit_clear
    tags: ["norm:svade_access_ad_bit_clear"]
